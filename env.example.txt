# Copy this file to ".env" and fill in real values. Never commit ".env".

# ---- Flask ----
SECRET_KEY=change-this-to-a-long-random-string
DATABASE_URL=sqlite:///app.db

# ---- Initial owner account (used once by seed.py) ----
OWNER_USERNAME=Rimon@340
OWNER_PASSWORD=837299
OWNER_NAME=Owner

# ---- Password reset ----
# OTP for ANY account's "forgot password" flow is sent to BOTH inboxes below.
RESET_OTP_EMAIL=ramansin888888@gmail.com
RESET_OTP_EMAIL_2=rimon200n@gmail.com

# SMTP settings used to actually send that OTP email (fill with a real mail account,
# e.g. a Gmail "App Password" - never your normal Gmail password).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=

# ---- Einpays merchant settings ----
# See README.md "Where to put your keys / IDs" section for exact line numbers.
EINPAYS_BASE_URL=https://pay18.einpays.com
EINPAYS_CLIENT_ID=528
EINPAYS_COUNTRY_ID=1
EINPAYS_CURRENCY_ID=3

# Confirmed from the Einpays "Common Deposits API" PDF: the deposit (payin)
# create call is the "/api/v5/methods/get" endpoint.
EINPAYS_CREATE_DEPOSIT_ENDPOINT=/api/v5/methods/get
EINPAYS_METHODS_ENDPOINT=/api/v5/methods/get
EINPAYS_BALANCE_ENDPOINT=/api/v5/balance
EINPAYS_TXSTATUS_ENDPOINT=/api/v5/txstatus
EINPAYS_PAYOUT_GETFORM_ENDPOINT=/api/v5/payouts/getform
EINPAYS_PAYOUT_SUBMIT_ENDPOINT=/api/v5/payouts/submit

# Default QR / payment-link expiry, in minutes (owner can override per link).
DEFAULT_LINK_EXPIRY_MINUTES=10
